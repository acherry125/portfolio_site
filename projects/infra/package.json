{
  "name": "@portfolio/infra",
  "version": "1.0.0",
  "private": true,
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "preview": "tsc && pulumi preview",
    "deploy": "tsc && pulumi up"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
