import * as React from "react";
import Layout from "../components/layout";
const SaltPage = () => {
  return <Layout></Layout>;
};

export default SaltPage;

export const Head = () => <title>Salt</title>;
